import functools
import itertools
import sys
from pathlib import Path

# Shared helpers live in blueearth_cst/; make them importable regardless of the working
# directory by prepending this Snakefile's own directory to sys.path.
# See dev/r03/model-builder-design.md §3.
sys.path.insert(0, str(Path(workflow.basedir)))
from blueearth_cst.shared.snake_utils import DEFAULT_BASIN_INDEX, DEFAULT_HYDROGRAPHY, climate_store_spec, get_config, patch_psutil_windows_benchmark, rule_banner, warn_if_project_dir_in_repo

# Windows: make Snakemake's benchmark memory/IO/CPU metrics work (else all NA).
patch_psutil_windows_benchmark()

# read path of the config file (Snakemake records it from --configfile) so
# downstream R scripts can be handed the same path. Forwarding config_path is
# a repo convention.
config_path = workflow.configfiles[0]

# R01 schema
project_cfg = config["project"]
shared_cfg = config["shared"]
my_cfg = config["workflows"]["climate_projections"]

project_dir = get_config(project_cfg, "project_dir", optional=False)
# O-22: make the two-tier project_dir rule mechanical rather than
# documentary. Warns, never raises; test_case/ is the one exemption.
warn_if_project_dir_in_repo(project_dir, workflow.basedir)
DATA_SOURCES = get_config(project_cfg, "data_sources_climate", optional=False)

# The shared climate store's catalog is project.data_sources (deltares), NOT the
# CMIP6 catalog above: the store is a hydrography/meteo extraction, not a
# projections read. WF2 therefore reads BOTH catalogs — a divergence from the
# pre-v2.0 workflow, which read only data_sources_climate.
STORE_DATA_SOURCES = get_config(project_cfg, "data_sources", optional=False)

# Shared — the model-free basin delineation the climate store extracts against.
basin_cfg = shared_cfg["basin"]
model_region = get_config(basin_cfg, "region", optional=False)
basin_hydrography = get_config(basin_cfg, "hydrography", DEFAULT_HYDROGRAPHY)
basin_index = get_config(basin_cfg, "basin_index", DEFAULT_BASIN_INDEX)
historical_window = get_config(shared_cfg, "historical_window", optional=False)
clim_source = get_config(shared_cfg, "clim_historical", optional=False)

clim_project = get_config(my_cfg, "clim_project", optional=False)
models = get_config(my_cfg, "models", optional=False)
scenarios = get_config(my_cfg, "scenarios", optional=False)
members = get_config(my_cfg, "members", optional=False)
variables = get_config(my_cfg, "variables", optional=False)

start_month_hyd_year = get_config(my_cfg, "start_month_hyd_year", "Jan")
time_horizon_hist = get_config(my_cfg, "historical_year_range", optional=False)
future_horizons = get_config(my_cfg, "future_horizons", optional=False)

save_grids = get_config(my_cfg, "save_grids", False)

clim_project_dir = f"{project_dir}/climate_projections/{clim_project}"

# ONE producer contract, built here and identically in Snakefile_model_creation
# and Snakefile_climate_experiment, and splatted into rule 2.11 / 1.10 / 3.02.
# Everything content- or execution-determining (script, the single catalog
# input, outputs, params) comes from this object; only message/log/benchmark are
# workflow-local. tests/test_climate_store_contract.py parses all three
# workflows and fails on ANY other difference.
#
# WF2 declares this producer to obtain store_region.geojson — a model-free
# delineated polygon — so the workflow no longer depends on wf1's
# hydrology_model/staticgeoms/region.geojson (design D2/A1, goal G2). The
# gridded extraction it also produces is NOT read by wf2 v2.0 (design N7); that
# cost is the accepted, stated price of A1, and it is a no-op whenever wf1 or
# wf3 has already built the store.
CLIMATE_STORE = climate_store_spec(
    project_dir=project_dir,
    model_region=model_region,
    clim_source=clim_source,
    historical_window=historical_window,
    data_sources=STORE_DATA_SOURCES,
    hydrography=basin_hydrography,
    basin_index=basin_index,
)
region_path = CLIMATE_STORE.outputs["region_geojson"]

# --- series identity (migration step 2b; design §5.3, D9, D12) ----------------
# The series files below stop being temp() and become a persistent product, so
# they need an identity: "was this derived from exactly the inputs the current
# config implies?" Everything knowable at PARSE time goes into the rules' params,
# where Snakemake's params rerun-trigger can see it. The polygon fingerprint is
# deliberately NOT here — on a fresh project the polygon does not exist yet, and
# a param flipping from absent to a real value on the second invocation would
# re-derive every series once for nothing (design §6.14). The reduce job folds it
# in and stamps the result on what it writes.
from blueearth_cst.projections import series_identity as _si

# The store index sits beside the catalog that generated it; both come from one
# crawl and carry an equal crawled_on (D12). Derived rather than configured: a
# separately-configurable path would let the two drift.
STORE_INDEX = str(Path(DATA_SOURCES).parent / "cmip6_store_index.json")

# ENUMERATED reducer modules, not "all of blueearth_cst" (risk-03): Snakemake's
# code trigger tracks the rule's script body, not the modules it imports, so a
# change to the reduction logic would otherwise reuse every cached series. Listed
# here so the enumeration is reviewable in a diff.
REDUCER_MODULES = [
    str(Path(workflow.basedir) / "blueearth_cst" / "projections" / "get_stats_climate_proj.py"),
    str(Path(workflow.basedir) / "blueearth_cst" / "projections" / "series_identity.py"),
]
REDUCER_HASH = _si.module_hash(REDUCER_MODULES)


#: WF2's spatial buffer around the region bbox, in degrees. Previously a bare
#: `buffer = 1` inside the script; named here because it is a digest component
#: (design §5.3 item 4) and a documented sampling choice, not an accident.
REGION_BUFFER_DEGREES = 1.0


@functools.lru_cache(maxsize=None)
def series_digest_components(model, experiment):
    """Parse-time digest components for one (model, experiment) series.

    Memoized: the generated catalog is ~3 900 lines and Snakemake evaluates a
    params callable once per job per param, so an unmemoized parse would re-read
    it on every lookup. Keyed on (model, experiment) — every other input is
    module-level and fixed for the run.

    ``members`` is the config LIST, not one member: at step 2b it is still looped
    inside one job, so a single output covers every member and the identity spans
    all of them. Step 3 makes it a wildcard, at which point the list has one
    element and this shape is unchanged.
    """
    catalog_entry = f"{clim_project}_{model}_{experiment}_{{member}}"
    entry = _si.load_catalog_entry(DATA_SOURCES, catalog_entry)
    return _si.digest_components(
        catalog_entry=catalog_entry,
        entry=entry,
        members=members,
        pins_by_member={
            m: _si.load_pins(STORE_INDEX, catalog_entry, m) for m in members
        },
        buffer_degrees=REGION_BUFFER_DEGREES,
        variable_spec=variables,
        experiment=experiment,
        reducer_module_hash=REDUCER_HASH,
    )

### Dictionary elements from the config based on wildcards
def get_horizon(wildcards):
    return config["workflows"]["climate_projections"]["future_horizons"][wildcards.horizon]

# Rule numbering (comment headers + log/benchmark filenames) uses `W.NN` =
# workflow.step in DEFINITION order — a reference aid ("Rule 2.4" = monthly_change),
# not execution order (the DAG fans out over {model}/{scenario}/{horizon}).
# Convention: dev/conventions/naming.md.

# 2.00  all — target aggregator: change-factor summaries + projection plots
rule all:
    input:
        (clim_project_dir + "/summary/annual_change_scalar_stats_summary.nc"),
        (clim_project_dir + "/summary/annual_change_scalar_stats_summary.csv"),
        (clim_project_dir + "/summary/annual_change_scalar_stats_summary_mean.csv"),
        stats_change_plt = (clim_project_dir + "/plots/projected_climate_statistics.png"),
        precip_plt = (clim_project_dir + "/plots/precipitation_anomaly_projections_abs.png"),
        temp_plt = (clim_project_dir + "/plots/temperature_anomaly_projections_abs.png"),
        snake_config = f"{project_dir}/config/runs/snake_config_climate_projections.yml",
        # one merged log per fan-out rule (gathered from the per-model _parts logs)
        stats_hist_log = f"{project_dir}/logs/2.02_monthly_stats_hist.log",
        stats_fut_log = f"{project_dir}/logs/2.03_monthly_stats_fut.log",
        change_log = f"{project_dir}/logs/2.04_monthly_change.log",
        benchmarks = f"{project_dir}/benchmarks/wf2_benchmarks.md",

# Retained as stale insurance pending removal task; dry-run on the current pinned
# Snakemake shows this directive constrains nothing on the tests fixture and a
# reduced config (see dev/r04/climate-projections-design.md §3). Removal deferred
# to a task that first encodes the ambiguity-sensitive config shapes as regression tests.
ruleorder: monthly_stats_hist > monthly_stats_fut > monthly_change > monthly_change_scalar_merge

# 2.11  extract_climate_grid — the SHARED historical-climate store producer
# (R07 B1). This declaration and rule 1.10 (Snakefile_model_creation) / rule
# 3.02 (Snakefile_climate_experiment) are the same rule: identical name, script,
# single catalog input, outputs and params, all splatted from CLIMATE_STORE.
# Only message/log/benchmark are workflow-local.
#
# WF2 consumes only `store_region.geojson` from it (design D2/A1). The gridded
# extraction is not read here — see the CLIMATE_STORE comment above.
#
# DO NOT add a workflow-local input or param here: an asymmetric input set
# re-creates the wf1<->wf3 re-extraction oscillation (design P2(b), ext1-02) and
# is forbidden. The catalog input is deliberately PLAIN, not ancient(): it is
# the store's freshness boundary (ext2-01).
rule extract_climate_grid:
    message: rule_banner("2.11", "extract_climate_grid")
    input:
        **CLIMATE_STORE.inputs,
    params:
        **CLIMATE_STORE.params,
    output:
        **CLIMATE_STORE.outputs,
    log:
        f"{project_dir}/logs/2.11_extract_climate_grid.log",
    benchmark:
        f"{project_dir}/benchmarks/_parts/2.11_extract_climate_grid.tsv",
    script:
        CLIMATE_STORE.script

# 2.01  copy_config — copy snake config into project_dir/config
rule copy_config:
    message: rule_banner("2.01", "copy_config")
    input:
        config_snake = config_path,
    params:
        data_catalogs = DATA_SOURCES,
        workflow_name = "climate_projections",
        config_dir = f"{project_dir}/config",
    output:
        config_snake_out = f"{project_dir}/config/runs/snake_config_climate_projections.yml",
    script:
        "blueearth_cst/model/copy_config_files.py"

# 2.02  monthly_stats_hist — mean monthly stats for the historical period, per
# {model} (grid to netcdf + grid-averaged monthly time series).
rule monthly_stats_hist:
    message: rule_banner("2.02", "monthly_stats_hist")
    input:
        region_path = region_path,
    output:
        #stats_nc_hist = (clim_project_dir + "/historical_stats_{model}.nc"),
        # PERSISTENT (step 2b): no longer temp(). The series is a product, and the
        # run that produced it may have cost hours while the file is ~17 KB. Its
        # reuse is gated on the cst_series_digest attribute, not on its presence.
        stats_time_nc_hist = (clim_project_dir + "/historical_stats_time_{model}.nc"),
    params:
        catalog_path = DATA_SOURCES,
        project_dir = f"{project_dir}",
        name_scenario = "historical",
        name_members = members,
        name_model = "{model}",
        name_clim_project = clim_project,
        variables = variables,
        save_grids = save_grids,
        # step 2b identity: parse-time digest components + the declared
        # acquisition window. future_horizons is deliberately absent (goal G5).
        digest_components = lambda wildcards: series_digest_components(wildcards.model, "historical"),
        acquisition_window = list(_si.acquisition_window("historical")),
        store_index = STORE_INDEX,
        buffer_degrees = REGION_BUFFER_DEGREES,
    log:
        project_dir + "/logs/_parts/2.02_monthly_stats_hist/{model}.log",
    benchmark:
        project_dir + "/benchmarks/_parts/2.02_monthly_stats_hist/{model}.tsv",
    script: "blueearth_cst/projections/get_stats_climate_proj.py"

# 2.03  monthly_stats_fut — mean monthly stats per {model}_{scenario} (future),
# chained after 2.02 (grid to netcdf + grid-averaged monthly time series).
rule monthly_stats_fut:
    message: rule_banner("2.03", "monthly_stats_fut")
    input:
        # The hist->fut ordering edge is GONE (step 2b). get_stats_climate_proj.py
        # never opened that file -- it reads only input.region_path -- so the edge
        # serialized the whole fan-out for no data reason. The real reason for it
        # was almost certainly the unguarded os.mkdir it protected; the script now
        # uses makedirs(exist_ok=True).
        region_path = region_path,
    output:
        # PERSISTENT (step 2b): see the note on rule 2.02.
        stats_time_nc = (clim_project_dir + "/stats_time-{model}_{scenario}.nc"),
    params:
        catalog_path = DATA_SOURCES,
        project_dir = f"{project_dir}",
        name_scenario = "{scenario}",
        name_members = members,
        name_model = "{model}",
        name_clim_project = clim_project,
        variables = variables,
        save_grids = save_grids,
        digest_components = lambda wildcards: series_digest_components(wildcards.model, wildcards.scenario),
        acquisition_window = lambda wildcards: list(_si.acquisition_window(wildcards.scenario)),
        store_index = STORE_INDEX,
        buffer_degrees = REGION_BUFFER_DEGREES,
    log:
        project_dir + "/logs/_parts/2.03_monthly_stats_fut/{model}_{scenario}.log",
    benchmark:
        project_dir + "/benchmarks/_parts/2.03_monthly_stats_fut/{model}_{scenario}.tsv",
    script: "blueearth_cst/projections/get_stats_climate_proj.py"

# 2.04  monthly_change — change factors per {model}_{scenario}_{horizon}
rule monthly_change:
    message: rule_banner("2.04", "monthly_change")
    input:
        # ancient() DROPPED (step 2b, design D9): with a persistent series store,
        # suppressing the mtime trigger here is exactly the hole ext2-01 found --
        # a re-derived series would not re-trigger its consumer. The consumer also
        # asserts each series' digest in-job, which is the backstop that holds
        # regardless of how Snakemake was invoked.
        stats_time_nc_hist = (clim_project_dir + "/historical_stats_time_{model}.nc"),
        stats_time_nc = (clim_project_dir + "/stats_time-{model}_{scenario}.nc"),
        # D9: stage B recomputes every expected digest INCLUDING the current
        # polygon fingerprint, so it needs the polygon itself.
        region_path = region_path,
    output:
        stats_nc_change = temp(clim_project_dir + "/annual_change_scalar_stats-{model}_{scenario}_{horizon}.nc"),
    params:
        clim_project_dir = f"{clim_project_dir}",
        # step 2b backstop: the expected digest of each input series, recomputed
        # from the same parse-time components the producer used.
        digest_components_hist = lambda wildcards: series_digest_components(wildcards.model, "historical"),
        digest_components_fut = lambda wildcards: series_digest_components(wildcards.model, wildcards.scenario),
        start_month_hyd_year = start_month_hyd_year, 
        name_model = "{model}",
        name_scenario = "{scenario}",
        name_horizon = "{horizon}",
        time_horizon_hist = time_horizon_hist,
        time_horizon_fut = get_horizon,
        save_grids = save_grids,
        stats_path_hist = (clim_project_dir + "/historical_stats_{model}.nc"),
        stats_path = (clim_project_dir + "/stats-{model}_{scenario}.nc"),
    log:
        project_dir + "/logs/_parts/2.04_monthly_change/{model}_{scenario}_{horizon}.log",
    benchmark:
        project_dir + "/benchmarks/_parts/2.04_monthly_change/{model}_{scenario}_{horizon}.tsv",
    script: "blueearth_cst/projections/get_change_climate_proj.py"

# 2.05  monthly_change_scalar_merge — merge all change factors into one summary
rule monthly_change_scalar_merge:
    message: rule_banner("2.05", "monthly_change_scalar_merge")
    input:
        stats_nc_change = ancient(expand((clim_project_dir + "/annual_change_scalar_stats-{model}_{scenario}_{horizon}.nc"), model = models, scenario = scenarios, horizon = future_horizons)),
    output:
        stats_change_summary = (clim_project_dir + "/summary/annual_change_scalar_stats_summary.nc"),
        stats_change_summary_csv = (clim_project_dir + "/summary/annual_change_scalar_stats_summary.csv"),
        stats_change_summary_csv_mean = (clim_project_dir + "/summary/annual_change_scalar_stats_summary_mean.csv"),
        stats_change_plt = (clim_project_dir + "/plots/projected_climate_statistics.png"),
    params:
        clim_project_dir = f"{clim_project_dir}",
        horizons = future_horizons,
    log:
        f"{project_dir}/logs/2.05_monthly_change_scalar_merge.log",
    benchmark:
        f"{project_dir}/benchmarks/_parts/2.05_monthly_change_scalar_merge.tsv",
    script: "blueearth_cst/projections/get_change_climate_proj_summary.py"

# 2.06  plot_climate_proj_timeseries — plot projected anomaly time series
rule plot_climate_proj_timeseries:
    message: rule_banner("2.06", "plot_climate_proj_timeseries")
    input:
        stats_change_summary = (clim_project_dir + "/summary/annual_change_scalar_stats_summary.nc"),
        stats_time_nc_hist =[(clim_project_dir + f"/historical_stats_time_{mod}.nc") for mod in models],
        stats_time_nc = expand((clim_project_dir + "/stats_time-{model}_{scenario}.nc"), model = models, scenario = scenarios),
    params:
        clim_project_dir = f"{clim_project_dir}",
        scenarios = scenarios,
        horizons = future_horizons,
        save_grids = save_grids,
        change_grids = [(clim_project_dir + f"/monthly_change_mean_grid-{mod}_{sc}_{hz}.nc") for mod,sc,hz in list(itertools.product(models,scenarios,future_horizons))],
    output:
        precip_plt = (clim_project_dir + "/plots/precipitation_anomaly_projections_abs.png"),
        temp_plt = (clim_project_dir + "/plots/temperature_anomaly_projections_abs.png"),
        timeseries_csv = (clim_project_dir + "/timeseries/gcm_timeseries.nc"),
    log:
        f"{project_dir}/logs/2.06_plot_climate_proj_timeseries.log",
    benchmark:
        f"{project_dir}/benchmarks/_parts/2.06_plot_climate_proj_timeseries.tsv",
    script: "blueearth_cst/projections/plot_proj_timeseries.py"

# --- log gather ---------------------------------------------------------------
# 2.02/2.03/2.04 run one job per model (parallel-safe per-model logs under
# logs/_parts/). These gather steps run once per stage, after all that stage's
# jobs, concatenating the per-model parts into a single logs/<rule>.log,
# regenerated fresh each run. Inputs are the stage's .nc outputs (all model jobs
# done); the exact part paths are passed via params so stale parts are excluded.

# gather 2.02 per-model logs -> 2.02_monthly_stats_hist.log
rule gather_stats_hist_logs:
    message: rule_banner("2.07", "gather_stats_hist_logs")
    input:
        expand(clim_project_dir + "/historical_stats_time_{model}.nc", model=models),
    output:
        f"{project_dir}/logs/2.02_monthly_stats_hist.log",
    params:
        parts = expand(
            project_dir + "/logs/_parts/2.02_monthly_stats_hist/{model}.log",
            model=models,
        ),
    script: "blueearth_cst/shared/merge_logs.py"

# gather 2.03 per-model logs -> 2.03_monthly_stats_fut.log
rule gather_stats_fut_logs:
    message: rule_banner("2.08", "gather_stats_fut_logs")
    input:
        expand(
            clim_project_dir + "/stats_time-{model}_{scenario}.nc",
            model=models, scenario=scenarios,
        ),
    output:
        f"{project_dir}/logs/2.03_monthly_stats_fut.log",
    params:
        parts = expand(
            project_dir + "/logs/_parts/2.03_monthly_stats_fut/{model}_{scenario}.log",
            model=models, scenario=scenarios,
        ),
    script: "blueearth_cst/shared/merge_logs.py"

# gather 2.04 per-model logs -> 2.04_monthly_change.log
rule gather_change_logs:
    message: rule_banner("2.09", "gather_change_logs")
    input:
        expand(
            clim_project_dir + "/annual_change_scalar_stats-{model}_{scenario}_{horizon}.nc",
            model=models, scenario=scenarios, horizon=future_horizons,
        ),
    output:
        f"{project_dir}/logs/2.04_monthly_change.log",
    params:
        parts = expand(
            project_dir + "/logs/_parts/2.04_monthly_change/{model}_{scenario}_{horizon}.log",
            model=models, scenario=scenarios, horizon=future_horizons,
        ),
    script: "blueearth_cst/shared/merge_logs.py"

# --- benchmark gather ---------------------------------------------------------
# Merge WF2's per-rule benchmark parts (benchmarks/_parts/2.*) into one
# benchmarks/wf2_benchmarks.tsv (rule column + TOTAL row) once the summary +
# plots are built (all WF2 rules ran).
rule gather_benchmarks:
    message: rule_banner("2.10", "gather_benchmarks")
    input:
        (clim_project_dir + "/summary/annual_change_scalar_stats_summary.nc"),
        (clim_project_dir + "/plots/projected_climate_statistics.png"),
        (clim_project_dir + "/plots/precipitation_anomaly_projections_abs.png"),
    output:
        f"{project_dir}/benchmarks/wf2_benchmarks.md",
    params:
        parts_dir = f"{project_dir}/benchmarks/_parts",
        workflow_num = 2,
    script: "blueearth_cst/shared/merge_benchmarks.py"
