---
title: Show the per-member wflow log falsifier actually failing with `path_log` unset
type: todo-item
status: blocked
effort: 1
area: wf3 logging
origin: R7
queue: 4
created: 2026-08-07
updated: 2026-08-11
---

> [!warning] Blocked on a run, not a decision — 2026-08-09
> Reviewed with the rest of the logging cluster (`t2608071202`, `t2608071219`)
> and separated from it: those two turned out to be one shared code fix in
> `snake_utils.tee_to_log`, while this needs a **real concurrent WF3 batch** to
> show the falsifier failing with `path_log` unset. That is a pipeline run, and
> AGENTS.md reserves those for the primary checkout.
>
> **Trigger.** The next WF3 run that happens anyway — R12 is the expected
> moment, the same sitting `t2608090907a` (ruff format stage 2) and
> `t2608091116`'s remaining half are parked against. Unset `logging.path_log`
> for one batch, confirm the members collide on a shared `log.txt`, restore it.
> Nothing here justifies scheduling a run of its own.

> [!note] Overview
> **What** — Show the per-member wflow log falsifier actually failing with `path_log` unset.
> **Why** — The fix shipped on reasoning about concurrency; until the falsifier fails without it, the concurrency claim is untested.
> **Effort** — Small but needs a real concurrent WF3 batch, so it rides with a run that happens anyway.

## Progress

- [ ] <first step>

## Refs

- Migrated from `dev/followups.md` item **R7-8** on 2026-08-07, when the board replaced it. Prose below is that
  entry verbatim; it is the reproducible context, not a summary.

## Detail

**[R7-8] ~~wflow writes `log.txt` beside the run TOML.~~ FIXED 2026-08-03 by
R9 P2 commit 3.** `downscale_climate_forcing.py` sets `logging.path_log` per
member, so each logs to its own file. It shipped in the same commit as the
`rlz_<n>/` flattening and not after: flattening put every member's TOML in one
shared `config/`, where the wflow default `"log.txt"` would have had all
members writing one file concurrently — a cosmetic became a correctness
problem the moment realizations stopped owning a directory.

**Still owed:** the concurrency falsifier has never been shown to FAIL with
`path_log` unset. Distinct pointers per member are unit tested via
`snake_utils.member_pointer_base`; content attribution under a real concurrent
batch still needs a run.
