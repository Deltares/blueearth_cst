---
title: Land ADR 0005 stage 2 — reformat the 51 blueearth_cst modules
type: todo-item
status: blocked
effort: 1
area: formatting
origin: R7
queue:
created: 2026-08-09
updated: 2026-08-11
---

> [!note] Overview
> **What** — `ruff format` had never been run over `blueearth_cst/`: an entry in
> `[tool.ruff.format] exclude` held those modules out while the rest of the repo
> was formatted and gated. The code half is now done — the exclusion is deleted
> and the modules are formatted. What is left is the proof that it changed no
> numbers.
> **Why** — The exclusion was temporary debt this ADR named as its own residual
> risk: a formatter gate that skips the largest source tree in the repo lets that
> tree drift, and every day it drifts the eventual reformat gets bigger.
> **Effort** — The remaining step is not work, it is a comparison someone runs
> after the next full pipeline re-run: `check_baseline.py check` must report zero
> differences. The unknown is only when that re-run happens.

## Progress

*Blocked — nothing is left to write. The `check_baseline.py check` below needs a
full pipeline re-run to compare against, and none is scheduled. Unblocks at the
next one (R12, or any `--forceall` run).*

- [x] ~~Wait for a scheduled full re-run~~ — overtaken 2026-08-11: the owner
      released this early rather than hold the debt for an unscheduled date
- [x] Delete `blueearth_cst/**` from `[tool.ruff.format] exclude`, leaving `*.md`
      — done; the formatter's scope is now the whole repo
- [x] `ruff format` over the tree — 53 modules, not the 51 last measured; the set
      grew again while it waited
- [x] Commit alone — `3f7c660`
- [x] In the same sitting, land `t2608091116`'s import-sorting half — `73c57df`,
      committed immediately before this one, so one invalidation covers both
- [ ] `check_baseline.py check` after the next full re-run — formatting changes
      no artifact, so it must compare equal; that equality is the whole point

## Refs

- `dev/decisions/0005-adopt-ruff-format-in-two-stages.md` — the ruling. Its
  Revisions log carries the 2026-08-11 landing and why it left its R12 slot.
- `dev/tasks/t2608071220-r10-14.md` (R10-14, watch) — the cost model this item
  was scheduled around: a one-line edit inside a `script:` module scheduled 42
  jobs, because Snakemake hashes the whole script text for its `code` rerun
  trigger. That invalidation has now been spent; it reaches every `project_dir`
  on disk, not only this repo's fixture, so the next run of any project is the
  one that pays it.
- Closed predecessor: `t2608071212` (R7-23), in `dev/LOG.md` — the ruling and
  stage 1.
- Landed while another session was live in the same worktree, so both commits
  were made by explicit pathspec (`pyproject.toml blueearth_cst/`) rather than
  from the index, which held that session's staged deletion.
