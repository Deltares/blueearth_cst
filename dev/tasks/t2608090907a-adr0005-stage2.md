---
title: Land ADR 0005 stage 2 — reformat the 51 blueearth_cst modules
type: todo-item
status: blocked
effort: 1
area: formatting
origin: R7
queue:
created: 2026-08-09
updated: 2026-08-09
---

> [!note] Overview
> **What** — Delete the `blueearth_cst/**` entry from `[tool.ruff.format] exclude`
> in `pyproject.toml`, run `pixi run format`, commit it alone.
> **Why** — Stage 1 armed the gate for everything else; these 51 files are the
> only unformatted source left, and the exclusion holding them out is temporary
> debt ADR 0005 named as its own residual risk.
> **Effort** — Small. The edit is two lines and one command; the *scheduling* is
> the whole difficulty.

## Progress

*Blocked on a trigger, not on a decision — the ruling was taken 2026-08-09 and
stage 1 landed the same day.*

**Trigger.** A full pipeline re-run is already scheduled: an R12 baseline
re-record, or any run launched with `--forceall`. Land this as its own
mechanical commit **immediately before** that run, so the invalidation it causes
is absorbed by work that was going to re-run everything anyway.

- [ ] Wait for a scheduled full re-run (R12 is the expected moment)
- [ ] Delete `blueearth_cst/**` from `[tool.ruff.format] exclude`, leaving `*.md`
- [ ] `pixi run format`, then `pixi run format-check` and `pytest tests/`
- [ ] Commit alone, immediately before the run
- [ ] `check_baseline.py check` after the run — formatting changes no artifact,
      so it must compare equal; that equality is the point of running it

## Refs

- `dev/decisions/0005-adopt-ruff-format-in-two-stages.md` — the ruling. Stage 2
  is its second half; the Decision section explains why the split exists.
- `dev/tasks/t2608071220-r10-14.md` (R10-14, watch) — the measured cost model: a
  one-line edit inside a `script:` module scheduled 42 jobs, because Snakemake
  hashes the whole script text for its `code` rerun trigger. All 51 files here
  are that layer, and the invalidation reaches every `project_dir` on disk, not
  only this repo's fixture.
- `pyproject.toml` `[tool.ruff.format]` — the exclusion to delete. It is
  `format`-scoped, so these files are already linted; only formatting is held.
- Closed predecessor: `t2608071212` (R7-23), in `dev/LOG.md` — the ruling and
  stage 1.
