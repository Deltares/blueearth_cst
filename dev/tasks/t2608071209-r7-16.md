---
title: Decide real packaging (O-14 decision 2) and unblock flit (O-16)
type: todo-item
status: backlog
effort: 2
area: packaging
origin: R7
queue: 9
created: 2026-08-07
updated: 2026-08-07
---

> [!note] Overview
> **What** — Decide real packaging (O-14 decision 2) and unblock flit (O-16).
> **Why** — O-16 stays blocked until the packaging decision has a superseding record, so the tooling contract cannot close.
> **Effort** — Medium, and mostly a decision plus a decision record rather than code.

## Progress

- [ ] <first step>

## Refs

- Migrated from `dev/followups.md` item **R7-16** on 2026-08-07, when the board replaced it. Prose below is that
  entry verbatim; it is the reproducible context, not a summary.

## Detail

**[R7-16] Tooling contract** *(O-14 decision 2 + O-16 are row `t260802d`)*.
**Resolved:** O-14 decision 1 (`ab781a5`) — a tool-config-only
`pyproject.toml`, no `[build-system]` / `[project]` / `[tool.pixi]`; and O-15
(`85d3178` → `81e0096`) — ruff adopted as the lint gate with
`select = ["E4","E7","E9","F"]` pinned explicitly, because ruff 0.16's
*default* selection is ~415 rules and must not be inherited by accident.
**Open:** O-14 decision 2 (real packaging) needs a superseding record in
`dev/decisions/`; O-16 (flit) stays gated on it. `ruff format` is configured
but deliberately unenforced — see R7-23.
