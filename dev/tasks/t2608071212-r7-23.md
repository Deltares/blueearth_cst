---
title: Rule on adopting `ruff format`, then land it in one mechanical commit
type: todo-item
status: blocked
effort: 2
area: formatting
origin: R7
queue: 12
created: 2026-08-07
updated: 2026-08-07
---

> [!note] Overview
> **What** — Rule on adopting `ruff format`, then land it in one mechanical commit.
> **Why** — It is configured but unenforced, so the tree drifts while the tool sits there implying it does not.
> **Effort** — The edit is mechanical across 136 files; the cost is the baseline re-record and the churn, which is why it needs a ruling first.

## Progress

*Blocked — an owner ruling on adopting `ruff format`; the churn is 136 of 276 files and grows with every unformatted commit.*

- [ ] Obtain the owner ruling on adopting `ruff format`
- [ ] Land the reformat as one mechanical commit with no other change in it
- [ ] Re-record the baseline afterwards — the reformat rewrites files the gate reads

## Refs

- The ruling this item waits on is drafted as ADR 0005 (`dev/decisions/0005-adopt-ruff-format-in-two-stages.md`), status `proposed`. It re-measures the churn on `main` at `5bc3d6a` -- 169 of 202 tracked source files, not the 136 of 276 recorded below -- and proposes a two-stage split so the formatting commit does not force a full pipeline re-run on every `project_dir` on disk.
- Migrated from `dev/followups.md` item **R7-23** on 2026-08-07, when the board replaced it. Prose below is that
  entry verbatim; it is the reproducible context, not a summary.

## Detail

**[R7-23] `ruff format` is configured but not enforced.** *Row `t260802f`
(blocked on an owner ruling).* **Re-measured 2026-08-02: now 136 of 276 files**
would be reformatted (118 at R7) — the churn grows with every unformatted
commit, so deferring has a running cost. Original entry: 118 of 262 files
would be reformatted, ~7.8k diff lines. That is a churn decision on its own
merits and was deliberately kept out of the O-15 lint adoption. If it is ever
taken, it should be a single mechanical commit with no other change in it, so
the diff stays reviewable — and note it would rewrite files the baseline gate
reads, so re-record afterwards. Likewise, the rule families left out of
`select` (`I` import sorting, `UP` pyupgrade, `B`/`SIM`/`PERF`/`RUF`) can each
be added later as its own reviewable commit; `I` alone is ~62 files.
